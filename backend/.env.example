# MongoDB Atlas (optional — local JSON fallback if missing)
MONGODB_URI=
MONGODB_DB=dietre

# Gemini (optional — mock parser if missing)
GEMINI_API_KEY=

# Host session cookie (optional in local demo)
AUTH_SECRET=

# Firebase Auth for hosts (optional — email mock login if missing)
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
